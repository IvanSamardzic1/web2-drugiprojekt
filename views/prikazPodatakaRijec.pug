extends layout

block layout-content
  div.View
    div.Message
      div.Title
        h1 Web-aplikacija za provjeru sigurnosti.
        h5 1. zadatak - SQL umetanje (SQL Injection)
        h6 Rezultati upita
  body
    h1 Popis korisnika
    table
      thead
        tr
          th Nadimak/Nickname
          th Ime
          th Prezime
          th Zanimanje
          th Godine
      tbody
        each user in data
          tr
            td= user.nadimak
            td= user.ime
            td= user.prezime
            td= user.zanimanje
            td= user.godine
            
  a(href='/sqlumetanjeRijec', class='button1') Povratak na prošlu stranicu